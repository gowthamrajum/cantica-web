# Telugu Community Church — https://cantica-web.onrender.com
#
# Everything a visitor might search for is open. The operator tools also carry
# a noindex meta tag, which is what actually keeps them out of results; the
# Disallow lines below stop crawlers spending fetches on them in the first
# place. Both are needed — a page that is never crawled can still be indexed
# from a link elsewhere, and only the meta tag prevents that.

User-agent: *
Allow: /

Disallow: /build
Disallow: /remote
Disallow: /notify
Disallow: /c/
Disallow: /live/

Sitemap: https://cantica-web.onrender.com/sitemap.xml
